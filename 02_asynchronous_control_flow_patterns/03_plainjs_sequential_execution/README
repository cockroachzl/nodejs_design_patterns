This sample demonstrates how to run a set of tasks in series.

To run the application, first install the dependencies:
  npm install

Then execute the 'spider' module by providing a valid url (including the protocol)
  node spider http://www.example.com

