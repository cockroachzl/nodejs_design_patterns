This sample demonstrates how to stream a file to a remote server, 
encrypting and compressing the data on the client, 
and decrypting and decompressing the data on the server.

To run the program first start the server:
  node gzipReceive

Then run the client selecting the file to send:
  node gzipSend <path to file> localhost
